You are an expert full-stack engineer. We have a critical navigation bug and a missing compliance asset on our landing pages that we need to fix right now. Please execute the following two updates perfectly without truncating any code:

1. FIX THE PERSISTENT 'NETWORK STATUS' LINK DEEP-ROUTING BUG:
In our main website footer, the 'Network Status' link under the 'OMNIDIFF NETWORK' column is set to 'href="/omnidiff#network-ledger"'. However, when clicked, it still lands at the top of the OmniDiff page instead of jumping to the ledger section. 
- Fix this by opening the OmniDiff page file and finding the main container element for the 'Global Network Status & Activity Ledger' section.
- Explicitly add the attribute id="network-ledger" to that section's outer HTML container element. 
- If the id is already present, ensure it is not being overridden by JavaScript or duplicate IDs, so that the browser natively snaps the viewport directly to the ledger when coming from the main page.

2. INJECT THE REGULATORY DISCLAIMER FOOTER TO THE MORPHIX SYSTEMS WEBSITE:
At the absolute bottom of our main Morphix website page (directly below our sub-footer copyright row), add an institutional compliance disclaimer block. Style it using small, low-contrast, highly professional typography that matches our signature dark mode look. 

Use this exact code structure for the bottom of the layout:

<!-- REGULATORY COMPLIANCE DISCLAIMER ROW -->
<div class="mt-8 pt-6 border-t border-gray-950 max-w-7xl mx-auto px-4 text-[10px] text-gray-600 leading-relaxed font-sans text-justify">
  <p class="mb-2">
    <span class="font-semibold text-gray-500">Disclaimer:</span> All pre-launch incentives, including early renter lifetime discounts and premium hardware provider revenue share allocations described herein, represent promotional expressions of interest frameworks only. Final terms, priority routing metrics, and yield percentages are strictly subject to execution of definitive corporate platform contracts, service level agreements (SLAs), and verified hardware enrollment compliance parameters. Void where prohibited.
  </p>
  <p>
    All expressions of interest (EOIs) collected via invitation requests are non-binding. Morphix Systems Inc. reserves the right to modify network onboarding thresholds, hardware compatibility mandates, and geofencing routing protocols in accordance with regional compliance updates and localized data infrastructure statutes.
  </p>
</div>

Please review both files, apply the id attribute to the ledger section on the OmniDiff page, and mount this disclaimer block cleanly at the base of the Morphix footer. Do not leave any placeholder sections.